const {
    HASH, HASH_SIZE, BLOCK_SIZE,
} = require('@nhz.io/nw-treehash/lib/constants')

const ITEM_ID = 'abcdefghijklmnopabcdefghijklmnop'

const ITEM_VERSION = '1.2.3'

module.exports = {
    HASH, HASH_SIZE,
    BLOCK_SIZE,
    ITEM_ID, ITEM_VERSION,
}
